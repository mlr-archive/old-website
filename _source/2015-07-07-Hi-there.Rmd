---
layout: post
title: Hi there!
author: michel
---

This is just a test post to check if

* `R` is working:
  ```{r}
  set.seed(2)
  mean(rnorm(100))
  ```
* Plots are not broken
  ```{r example-plot}
  plot(1:11)
  ```
